# 精灵日程管理系统功能文档

## 1. 系统概览

精灵日程管理系统是一个基于AI的智能日程管理系统，通过5个拟人化精灵帮助用户管理时间和任务。系统利用大语言模型（LLM）实现智能对话、任务拆解、时间协商等功能，为用户提供个性化的日程管理体验。

### 1.1 核心价值
- **智能任务管理**：自动识别、拆解和安排任务
- **拟人化交互**：通过5个不同性格的精灵提供个性化服务
- **智能协商**：当任务时间冲突时，精灵们会进行协商并提供解决方案
- **个性化推荐**：基于用户行为和偏好推荐时间槽
- **数据可视化**：通过树形结构展示用户任务完成情况

## 2. 系统架构

### 2.1 技术栈
- **后端**：Python 3.13, FastAPI, SQLAlchemy, PostgreSQL
- **AI**：LLM 集成（通过 llm_client）
- **任务调度**：Celery
- **前端**：（前端代码未在当前代码库中）

### 2.2 目录结构
```
app/
├── ai/                # AI 相关模块
│   ├── spirits/       # 精灵实现
│   ├── prompts/       # 精灵提示词
│   ├── llm_client.py  # LLM 客户端
│   ├── negotiation.py # 协商引擎
│   └── task_parser.py # 任务解析器
├── models/            # 数据模型
├── schemas/           # 数据验证和序列化
├── services/          # 业务逻辑服务
├── routers/           # API 路由
├── middleware/        # 中间件
├── jobs/              # 定时任务
├── utils/             # 工具函数
├── config.py          # 配置
├── database.py        # 数据库连接
└── main.py            # 应用入口
```

## 3. 核心功能模块

### 3.1 AI 精灵系统

#### 3.1.1 精灵类型
系统包含5个不同领域的精灵，每个精灵有独特的性格和专长：
- **light**：灯光精灵（可能负责时间管理和日程安排）
- **water**：水精灵（可能负责健康和休息）
- **soil**：土壤精灵（可能负责基础任务和稳定）
- **air**：空气精灵（可能负责创意和灵感）
- **nutrition**：营养精灵（可能负责能量管理）

#### 3.1.2 精灵基础能力
每个精灵都具备以下能力：
- **对话**：与用户进行自然语言交互
- **任务拆解**：将复杂任务分解为可执行的子任务
- **时间协商**：在多精灵协商中为自己负责的任务争取时间
- **周评点评**：根据用户的周表现生成点评

### 3.2 对话系统

#### 3.2.1 私聊模式
- **功能**：用户与单个精灵进行一对一对话
- **实现**：通过 `/ai/spirits/{spirit_code}/chat` 接口
- **流程**：
  1. 用户发送消息
  2. 系统获取或创建对话会话
  3. 保存用户消息
  4. 精灵根据用户画像和对话历史生成回复
  5. 保存精灵回复
  6. 检测对话中是否包含任务建议
  7. 保存任务建议（如果有）
  8. 返回精灵回复和任务建议

- **Chat-to-Task 功能**：精灵会自动识别对话中的任务需求，并生成任务建议

#### 3.2.2 群聊模式（协商系统）
- **功能**：多个精灵围绕任务时间安排进行协商
- **实现**：通过 `/ai/negotiate` 接口，使用 SSE（Server-Sent Events）流式输出
- **协商流程**：
  1. **初始化**：加载任务数据和用户上下文
  2. **收集诉求**：各精灵声明时间诉求
  3. **检测冲突**：分析精灵间的时间冲突
  4. **主持协商**：主持人（系统）分析冲突并提出调停方案
  5. **精灵回应**：精灵们对调停方案进行回应
  6. **评估共识**：评估是否达成共识
  7. **循环协商**：最多进行3轮协商
  8. **用户决策**：如果未达成共识，提交用户决策
  9. **生成日程**：根据协商结果生成最终日程

- **SSE 事件类型**：
  - `spirit_message`：精灵发言
  - `orchestrator`：主持人调停
  - `consensus`：达成共识
  - `need_user_input`：需要用户决策
  - `error`：错误（含降级方案）
  - `done`：协商结束

### 3.3 任务管理

#### 3.3.1 任务解析
- **功能**：将用户的自然语言输入解析为任务
- **实现**：通过 `/ai/parse` 接口
- **流程**：使用任务解析器分析用户输入，提取任务信息（标题、时间、优先级等）

#### 3.3.2 任务拆解
- **功能**：将复杂任务分解为子任务
- **实现**：通过 `/ai/spirits/decompose` 接口
- **流程**：
  1. 获取任务信息
  2. 调用对应精灵的拆解能力
  3. 生成子任务列表
  4. 保存子任务

### 3.4 日程管理

#### 3.4.1 时间槽推荐
- **功能**：为新任务推荐可用时间槽
- **实现**：通过 `/ai/suggest-slot` 接口
- **流程**：基于用户日程和偏好，推荐合适的时间槽

#### 3.4.2 协商结果应用
- **功能**：将协商结果应用到日程
- **实现**：通过 `_apply_negotiation_schedule` 函数
- **流程**：
  1. 按日期分组协商结果
  2. 获取现有日程
  3. 合并协商结果（协商结果优先）
  4. 按时间排序
  5. 保存更新后的日程

## 4. 协商引擎深度分析

### 4.1 协商引擎架构

协商引擎是系统的核心组件，负责协调多个精灵之间的时间冲突。其设计采用状态机模式，包含以下状态：

1. **INIT**：初始化状态
2. **COLLECTING_CLAIMS**：收集精灵诉求
3. **DETECTING_CONFLICTS**：检测时间冲突
4. **MEDIATING**：主持人调停
5. **AWAITING_RESPONSES**：等待精灵回应
6. **EVALUATING**：评估共识
7. **CONSENSUS_REACHED**：达成共识
8. **NEED_USER_INPUT**：需要用户决策
9. **RESOLVED**：已解决
10. **FAILED**：协商失败

### 4.2 协商流程详解

1. **初始化阶段**：
   - 加载用户任务数据
   - 确定参与协商的精灵
   - 构建协商上下文

2. **协商循环**（最多3轮）：
   - **收集诉求**：各精灵声明时间诉求
   - **检测冲突**：分析精灵间的时间冲突
   - **主持协商**：主持人提出调停方案
   - **精灵回应**：精灵们对调停方案进行回应
   - **评估共识**：评估是否达成共识

3. **结果处理**：
   - 如果达成共识，生成日程
   - 如果未达成共识，生成备选方案供用户选择

4. **降级策略**：
   - 当LLM不可用时，按优先级 > 精灵权重 > 创建时间排序
   - 直接用调度器算法分配，不走对话流程

### 4.3 协商引擎的问题与改进空间

#### 4.3.1 现有问题
1. **冲突检测过于简单**：当前仅通过文本匹配检测时间冲突，可能导致漏检或误检
2. **协商逻辑不够灵活**：固定3轮协商，可能在某些情况下不够充分
3. **用户介入时机固定**：只有在3轮协商后才会请求用户介入
4. **缺乏历史协商数据利用**：未利用历史协商结果优化协商策略
5. **精灵个性体现不足**：不同精灵的协商风格差异不够明显

#### 4.3.2 改进建议
1. **增强冲突检测**：
   - 实现更精确的时间冲突检测算法
   - 考虑任务优先级、deadline等因素
   - 分析任务间的依赖关系

2. **优化协商流程**：
   - 动态调整协商轮数，根据冲突复杂度自动决定
   - 引入更智能的调停策略，考虑用户历史偏好
   - 增加精灵间的直接交互，而不仅仅是对主持人的回应

3. **改进用户介入机制**：
   - 允许用户在协商过程中随时介入
   - 提供更详细的协商过程可视化
   - 支持用户预设协商偏好

4. **利用历史数据**：
   - 记录和分析历史协商结果
   - 基于历史数据优化精灵的协商策略
   - 预测用户对不同协商结果的接受度

5. **增强精灵个性**：
   - 为每个精灵设计更独特的协商风格
   - 考虑精灵间的关系和互动历史
   - 引入情绪因素，使协商过程更自然

## 5. API 接口

### 5.1 AI 相关接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/v1/ai/parse` | POST | 自然语言解析，返回任务解析结果 |
| `/api/v1/ai/spirits/{spirit_code}/chat` | POST | 单精灵对话，支持多轮对话和Chat-to-Task |
| `/api/v1/ai/spirits/decompose` | POST | 手动触发任务拆解 |
| `/api/v1/ai/negotiate` | POST | 发起精灵协商，SSE流式输出 |
| `/api/v1/ai/negotiate/resolve` | POST | 用户介入协商，选择方案 |
| `/api/v1/ai/negotiate/status/{negotiation_id}` | GET | 查询协商状态 |
| `/api/v1/ai/suggest-slot` | POST | 为新任务推荐可用时间槽 |

### 5.2 其他核心接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/v1/auth/*` | - | 认证相关接口 |
| `/api/v1/users/*` | - | 用户管理接口 |
| `/api/v1/profile/*` | - | 用户画像管理接口 |
| `/api/v1/tasks/*` | - | 任务管理接口 |
| `/api/v1/schedule/*` | - | 日程管理接口 |
| `/api/v1/reports/*` | - | 报告生成接口 |
| `/api/v1/tree/*` | - | 任务树管理接口 |
| `/api/v1/fruits/*` | - | 成果管理接口 |
| `/api/v1/notifications/*` | - | 通知管理接口 |

## 6. 技术实现细节

### 6.1 LLM 集成
- 使用 `llm_client` 与大语言模型进行交互
- 支持 JSON 格式输出，确保结构化响应
- 实现了降级策略，当LLM不可用时使用默认响应

### 6.2 精灵系统
- 基于 `BaseSpirit` 抽象类实现具体精灵
- 支持从 `.md` 文件加载精灵提示词，按强度档位调整
- 每个精灵有独立的人格、说话风格和领域专长

### 6.3 会话管理
- 使用 `ConversationService` 管理对话会话
- 支持多轮对话历史保存
- 实现了 `ChatTaskSuggestion` 记录任务建议

### 6.4 数据模型
- 使用 SQLAlchemy ORM 进行数据库操作
- 主要模型包括：User, Task, Conversation, ChatTaskSuggestion等
- 支持异步数据库操作

## 7. 系统工作流程

### 7.1 典型用户场景

#### 场景1：添加任务
1. 用户通过自然语言描述任务
2. 系统解析任务信息
3. 精灵提供任务建议
4. 用户确认后创建任务
5. 系统推荐时间槽
6. 用户确认时间安排

#### 场景2：任务协商
1. 系统检测到任务时间冲突
2. 启动精灵协商流程
3. 精灵们进行多轮协商
4. 达成共识或提交用户决策
5. 生成最终日程安排

#### 场景3：任务拆解
1. 用户选择一个复杂任务
2. 系统调用对应精灵进行任务拆解
3. 精灵生成子任务列表
4. 用户确认子任务
5. 系统安排子任务时间

## 8. 总结与亮点回顾

### 8.1 系统亮点
1. **拟人化精灵**：通过5个不同性格的精灵提供个性化服务，增强用户体验
2. **智能协商**：创新的多精灵协商系统，自动解决时间冲突
3. **流式输出**：使用SSE实现协商过程的实时反馈
4. **个性化推荐**：基于用户画像和行为推荐时间槽
5. **Chat-to-Task**：从对话中自动识别任务需求
6. **降级策略**：确保系统在LLM不可用时仍能正常工作

### 8.2 应用价值
- **提高时间管理效率**：智能任务安排和时间优化
- **减轻决策负担**：精灵协商系统自动解决时间冲突
- **增强用户粘性**：拟人化交互和个性化服务
- **促进任务完成**：通过任务拆解和提醒提高任务完成率

### 8.3 未来发展方向
1. **增强精灵能力**：扩展精灵的领域知识和交互能力
2. **引入更多AI功能**：如智能总结、预测分析等
3. **优化用户界面**：提供更直观的协商过程可视化
4. **支持多平台**：扩展到移动应用和其他平台
5. **社区功能**：引入用户间的任务分享和协作

精灵日程管理系统通过创新的AI协商机制和拟人化交互，为用户提供了一种全新的时间管理体验。系统不仅能够智能安排任务，还能通过精灵协商解决时间冲突，大大减轻了用户的决策负担。随着AI技术的不断发展，系统有望在未来提供更加智能和个性化的服务。